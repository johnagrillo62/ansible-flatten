[0].name = Ensure older PHP packages are absent on reset for given PHP version
[0]["ansible.builtin.apt"].name = {{ item is search("php.*-") | ternary(item, php__version_absent + "-" + item) }}
[0]["ansible.builtin.apt"].state = absent
[0].loop = {{ q("flattened", php__server_api_packages
                           + php__base_packages
                           + php__packages
                           + php__group_packages
                           + php__host_packages
                           + php__dependent_packages) }}
