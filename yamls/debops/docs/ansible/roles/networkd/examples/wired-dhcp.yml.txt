networkd__units[0].name = wired-dhcp.network
networkd__units[0].comment = Configure any wired Ethernet interface via DHCP
networkd__units[0].raw = [Match]
Name=en*

[Network]
DHCP=yes

[DHCPv4]
UseDomains=true

networkd__units[0].state = present
