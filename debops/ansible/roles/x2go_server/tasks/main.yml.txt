[0].name = Ensure specified packages are in there desired state
[0]["ansible.builtin.apt"].name = {{ q("flattened", x2go_server__base_packages) }}
[0]["ansible.builtin.apt"].state = {{ "latest" if (x2go_server__deploy_state == "present") else "absent" }}
[0]["ansible.builtin.apt"].install_recommends = False
[0]["ansible.builtin.apt"].purge = True
[0].register = x2go_server__register_packages
[0].until = x2go_server__register_packages is succeeded
[0].when = (not ansible_check_mode)
