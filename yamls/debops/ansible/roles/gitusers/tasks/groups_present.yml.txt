[0].name = Create user groups without GIDs
[0]["ansible.builtin.group"].name = {{ item.group | default(item.name + gitusers_name_suffix) }}
[0]["ansible.builtin.group"].system = {{ item.systemgroup | default("no") }}
[0]["ansible.builtin.group"].state = present
[0].loop = {{ q("flattened", gitusers_list
                           + gitusers_group_list
                           + gitusers_host_list) }}
[0].when = (((item.name is defined and item.name) and (item.gid is undefined or (item.gid is defined and not item.gid))) and (item.state is undefined or (item.state is defined and item.state != 'absent')))
[1].name = Create user groups with GIDs
[1]["ansible.builtin.group"].name = {{ item.group | default(item.name + gitusers_name_suffix) }}
[1]["ansible.builtin.group"].system = {{ item.systemgroup | default("no") }}
[1]["ansible.builtin.group"].gid = {{ item.gid }}
[1]["ansible.builtin.group"].state = present
[1].loop = {{ q("flattened", gitusers_list
                           + gitusers_group_list
                           + gitusers_host_list) }}
[1].when = ((item.name is defined and item.name) and (item.gid is defined and item.gid) and (item.state is undefined or (item.state is defined and item.state != 'absent')))
