dependencies[0].role = kubernetes/kubeadm_common
dependencies[1].role = adduser
dependencies[1].user = {{ addusers.etcd }}
dependencies[1].when[0] = etcd_deployment_type == "kubeadm"
dependencies[1].when[1] = not (ansible_os_family in ["Flatcar", "Flatcar Container Linux by Kinvolk", "ClearLinux"] or is_fedora_coreos)
dependencies[2].role = network_plugin/calico_defaults
dependencies[3].role = etcd_defaults
