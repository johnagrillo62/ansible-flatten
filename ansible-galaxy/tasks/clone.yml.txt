~d0.name = Clone Galaxy
~d1.block = 
~d2.name = Update Galaxy to specified ref
~d3.git = 
~d4.dest = "{{ galaxy_server_dir }}"
~d4.force = "{{ galaxy_force_checkout }}"
~d4.depth = "{{ galaxy_clone_depth | default(omit) }}"
~d4.repo = "{{ galaxy_repo }}"
~d4.version = "{{ galaxy_commit_id }}"
~d4.executable = "{{ git_executable | default(omit) }}"
~d3.diff = "{{ galaxy_diff_mode_verbose }}"
~d3.register = __galaxy_git_update_result
~d3.notify = 
~d2.name = Report Galaxy version change
~d3.debug = 
~d4.msg = "Galaxy version changed from '{{ __galaxy_git_update_result.before }}' to '{{ __galaxy_git_update_result.after }}'"
~d3.changed_when = __galaxy_git_update_result is changed
~d3.when = __galaxy_git_update_result is changed
~d2.name = Include virtualenv setup tasks
~d3.import_tasks = virtualenv.yml
~d2.name = Remove orphaned .pyc files and compile bytecode
~d3.import_tasks = compile.yml
~d3.when = __galaxy_git_update_result is changed
~d1.remote_user = "{{ galaxy_remote_users.privsep | default(__galaxy_remote_user) }}"
~d1.become = "{{ true if galaxy_become_users.privsep is defined else __galaxy_become }}"
~d1.become_user = "{{ galaxy_become_users.privsep | default(__galaxy_become_user) }}"
