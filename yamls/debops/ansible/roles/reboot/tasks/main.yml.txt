[0].name = Check if reboot is required
[0]["ansible.builtin.stat"].path = /var/run/reboot-required
[0]["ansible.builtin.stat"].get_checksum = False
[0].register = reboot__register_required
[1].name = Reboot DebOps hosts if needed or requested
[1]["ansible.builtin.reboot"].boot_time_command = {{ reboot__boot_time_command }}
[1]["ansible.builtin.reboot"].search_paths = {{ (reboot__default_search_paths + reboot__search_paths) | flatten }}
[1]["ansible.builtin.reboot"].reboot_timeout = {{ reboot__timeout }}
[1].when = reboot__register_required.stat.exists | bool or reboot__force | bool
