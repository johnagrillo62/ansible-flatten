~d0.name = Ensure older PHP packages are absent on reset for given PHP version
~d1.ansible.builtin.apt = 
~d2.name = '{{ item is search("php.*-") | ternary(item, php__version_absent + "-" + item) }}'
~d2.state = 'absent'
~d1.loop = '{{ q("flattened", php__server_api_packages
