[0].name = Create the file to initialize the mongod Shard
[0].template = src=shard_init.j2 dest=/tmp/shard_init_{{ inventory_hostname }}.js
[0].delegate_to = {{ item }}
[0].with_items = groups.mongos_servers
[1].name = Add the shard to the mongos
[1].shell = /usr/bin/mongo localhost:{{ mongos_port }}/admin -u admin -p {{ mongo_admin_pass }} /tmp/shard_init_{{ inventory_hostname }}.js
[1].delegate_to = {{ item }}
[1].with_items = groups.mongos_servers
