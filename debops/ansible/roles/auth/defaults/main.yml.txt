auth_packages[0] = {{ "libpam-cracklib"
        if (ansible_distribution_release in ["stretch", "buster", "bullseye"])
        else [] }}
auth_pwhistory_remember = 5
auth_cracklib = {{ True
                   if (ansible_distribution_release in ["stretch", "buster", "bullseye"])
                   else False }}
