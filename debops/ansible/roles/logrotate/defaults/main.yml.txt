logrotate__enabled = True
logrotate__base_packages[0] = logrotate
logrotate__cron_period = daily
logrotate__default_period = weekly
logrotate__default_rotation = 4
logrotate__options = 
logrotate__group_options = 
logrotate__host_options = 
logrotate__default_options = create
{{ logrotate__default_period }}
rotate {{ logrotate__default_rotation }}
tabooext + .dpkg-divert
include /etc/logrotate.d

logrotate__default_config[0].log = /var/log/wtmp
logrotate__default_config[0].comment = No packages own wtmp or btmp, they will be managed directly
logrotate__default_config[0].options = missingok
monthly
create 0664 root utmp
rotate 1

logrotate__default_config[0].state = {{ "present"
               if (ansible_distribution_release in
                   (["stretch", "trusty", "xenial", "bionic"]))
               else "absent" }}
logrotate__default_config[1].log = /var/log/btmp
logrotate__default_config[1].options = missingok
monthly
create 0660 root utmp
rotate 1

logrotate__default_config[1].state = {{ "present"
               if (ansible_distribution_release in
                   (["stretch", "trusty", "xenial", "bionic"]))
               else "absent" }}
logrotate__combined_config = {{ logrotate__config
                                + logrotate__group_config
                                + logrotate__host_config
                                + logrotate__dependent_config }}
