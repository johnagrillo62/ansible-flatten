[0].name = Check the API servers are responding
[0].uri.url = https://{{ (access_ip if (ipv4_stack | default(true)) else access_ip6) | default(ansible_default_ipv4.address if (ipv4_stack | default(true)) else ansible_default_ipv6.address) | ansible.utils.ipwrap }}:{{ kube_apiserver_port | default(6443) }}/version
[0].uri.validate_certs = false
[0].uri.status_code = 200
[0].register = apiserver_response
[0].retries = 12
[0].delay = 5
[0].until = apiserver_response is success
[1].name = Check API servers version
[1].assert.that[0] = apiserver_response.json.gitVersion == ('v' + kube_version)
[1].assert.fail_msg = apiserver is {{ apiserver_response.json.gitVersion }}, expected {{ kube_version }}
